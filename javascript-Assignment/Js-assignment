//  -----------------------JavaScript Assignment---------------------------
/*

1-What is JavaScript?
Ans:It is an interpreted programming language with object-oriented capabilities. 
JavaScript was first known as LiveScript, but Netscape changed its name to JavaScript, possibly because of the excitement being generated by Java.

2-What is the use of isNaN function?
Ans:The isNaN () function is used to check whether a given value is an illegal number or not. It returns true if value is a NaN else returns false. 
It is different from the Number.isNaN () Method.

3-What is Neegative Infinity?
Ans:NEGATIVE_INFINITY is a static property of the Number object that is used to return the value that represents negative infinity. Because NEGATIVE_INFINITY is a property of the Number object, it must be invoked through the object called Number.

4-Which company developed JavaScript?
Ans:Netscape

5-What are undeclared and undefined variables?
Ans:Undefined variable means a variable has been declared but it does not have a value. 
Undeclared variable means that the variable does not exist in the program at all.

6- Write the code for adding new elements dynamically? 
Ans:New elements can be created in JS by using the createElement () method
document.createElement("<tagName>");  

7-What is the difference between ViewState and SessionState?
Ans: The basic difference between these two is that the ViewState is to manage state at the client’s end, making state management easy for end-user while SessionState manages state at the server’s end, making it easy to manage content from this end too. ViewState: It is maintained at only one level that is page-level.

8-What is === operator? 
Ans:=== operator compare datatype also not only value.

9-What are all the looping structures in JavaScript? 
Ans:
i) JavaScript For loop The JavaScript for loop iterates the elements for the fixed number of times. ...
ii) JavaScript while loop The JavaScript while loop iterates the elements for the infinite number of times. ...
iii) JavaScript do while loop The JavaScript do while loop iterates the elements for the infinite number of times like while loop. ...
iv) JavaScript for in loop

10-How can you convert the string of any base to an integer in JavaScript?
Ans:parseInt() and parseFloat

11-What is the function of the delete operator?
Ans:The delete operator removes a given property from an object. On successful deletion, it will return true , else false will be returned.

12-What are all the types of Pop up boxes available in JavaScript?
Ans:i)prompt ii)alret iii)confirm box this three types of popup boxes available

13-What is the use of Void (0)? 
Ans:Web Developers use javascript:void (0) because it is the easiest way to prevent the default behavior of a tag. 

14-How can a page be forced to load another page in JavaScript? 
Ans:You can use window.location object to load another page in JavaScript. 

15-What are the disadvantages of using innerHTML in JavaScript?
Ans:i)Inner HTML is slow
ii)Event handlers attached to any DOM element are preserved
iii)Replacement is done everywhere
iV)It is not possible to append innerHTML
V)Breaks the document
vi)Used for Cross-site Scripting


*/
if ("0") { alert( 'Hello'); }